## 📁 Файлы

✅ `count_hh_with_diagnosis_and_service.sql` — подсчёт историй болезни по диагнозу I63 и услуге A25.30.036.002 за заданный период.

> 🔍 **Как использовать:**
> 1. Откройте файл в DBeaver.
> 2. При запуске появятся два параметра: `date1` и `date2`.
> 3. Введите даты **без одинарных кавычек**:  
>    → `01.09.2025` (а не `'01.09.2025'`)
> 4. Скрипт автоматически обработает их корректно благодаря синтаксису `${date1}`.

> 💡 **Почему так?**  
> Использование `${param}` в DBeaver позволяет передавать значение как строку *до* выполнения запроса — это безопасно и удобно для конечного пользователя.

> ⚠️ **Важно:**  
> Формат даты — строго `DD.MM.YYYY`.  
> Пример: `01.09.2025`, `30.09.2025`.

---

## 🧩 Почему это важно?

Часто пользователи забывают ставить кавычки при вводе дат — это приводит к ошибкам и потере времени.  
Этот скрипт решает проблему, делая интерфейс **удобным и надёжным**.

---
