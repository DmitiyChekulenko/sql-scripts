🔗 INNER JOIN
Описание: Возвращает только те записи, где есть соответствие в обеих таблицах. Используется когда нужны данные, гарантированно присутствующие в связанных таблицах.

Пример: 

sql
SELECT l.CODE_LPU, jj.NUM_VALUE
FROM D_LPU l
INNER JOIN D_LPU_SERVICES s ON s.LPU = l.ID  -- Только ЛПУ с услугами
WHERE ss.SE_CODE = 'A09.05.130'

🔗 LEFT JOIN
Описание: Возвращает все записи из левой таблицы, даже если нет соответствий в правой.
NULL значения подставляются при отсутствии связей. 
Критически важен для отчетности - показывает все МО, включая те, где не найдены случаи.

Пример:

sql
SELECT l.CODE_LPU, COUNT(hh.ID) AS cases_count
FROM D_LPU l
LEFT JOIN D_HOSP_HISTORIES hh ON hh.LPU = l.ID  -- Все ЛПУ, даже без случаев
GROUP BY l.CODE_LPU

🔗 RIGHT JOIN
Описание: Возвращает все записи из правой таблицы и соответствующие записи из левой.
Аналог LEFT JOIN, но с измененным порядком таблиц. 
Редко использую - обычно заменяется на LEFT JOIN с переставленными таблицами.

Пример:

sql
SELECT l.CODE_LPU, hh.DATE_OUT
FROM D_HOSP_HISTORIES hh
RIGHT JOIN D_LPU l ON l.ID = hh.LPU  -- Все ЛПУ, даже без историй болезни
WHERE hh.DATE_OUT BETWEEN '${date1}' AND '${date2}'

⚠️ CROSS JOIN
На практики не использовал, т.к. не имеет практический смысл в медицинской отчетности
Описание: Создает декартово произведение - каждая строка первой таблицы соединяется с каждой строкой второй.
Не рекомендуется к использованию в производственных запросах из-за экспоненциального роста данных.

Опасность:
1000 записей × 1000 записей = 1 000 000 строк
Может привести к полной остановке БД (Знаю подобные случаи)

✅Более подробное использование JOIN в запросах отражено в файлах в папке: "Progects"
