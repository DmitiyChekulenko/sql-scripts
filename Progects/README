# 🧠 Подсчёт пациентов с диагнозом инсульта (I60–I69.8)

Этот скрипт определяет **уникальное количество пациентов**, которым в заданный период времени был поставлен диагноз из группы **«Инсульты»** по МКБ-10 (коды `I60`–`I69.8`).

Он учитывает **оба канала диагностики**:
- 🏥 **Амбулаторные приёмы** (диагнозы, выставленные на визитах),
- 🏨 **Стационарные случаи** (клинические диагнозы из историй болезни).

---

## 🔍 Зачем это нужно?

В реальной практике пациент может получить диагноз:
- на приёме у невролога (через `D_VIS_DIAGNOSISES`),
- или уже в стационаре, при оформлении истории болезни (`D_HOSP_HISTORIES`).

Если просто объединить таблицы без `UNION`, один и тот же пациент может быть посчитан дважды.  
**Решение:** использовать `UNION`, чтобы автоматически исключить дубли по `patient_id`.

---

## ⚙️ Как использовать

1. Откройте скрипт в **DBeaver**.
2. При запуске появятся два параметра:
   - `date1` — начало периода (например: `01.01.2025`)
   - `date2` — конец периода (например: `31.12.2025`)
3. Вводите даты **без кавычек** — благодаря синтаксису `${date1}`, DBeaver корректно подставит их как строки.

> ✅ Пример ввода:  
> `date1 = 01.01.2025`  
> `date2 = 31.12.2025`

> ⚠️ Формат даты: **`DD.MM.YYYY`** (обязательно!)

---

## 📊 Что возвращает?

Один столбец:  
- `patient_count` — количество **уникальных пациентов** с диагнозом инсульта за указанный период.

---

## 🧩 Технические детали

- Используется подзапрос с `UNION` для объединения двух источников диагнозов.
- `COUNT(DISTINCT patient_id)` гарантирует, что каждый пациент считается **только один раз**, даже если у него было несколько эпизодов.
- Диапазон МКБ-10: `I60` — `I69.8` (все подтипы инсульта по классификатору).

---
