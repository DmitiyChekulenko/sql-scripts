# Анализ выявления онкологических и сопутствующих заболеваний ЖКТ после высокого ПСА

## 📌 Описание

Данный SQL-скрипт реализует многоэтапный анализ данных с целью выявления пациентов, у которых:

1. **ПСА (простат-специфический антиген) ≥ 100 нг/мл**,
2. Возраст на момент анализа — **от 40 до 74 лет**,
3. Исследование проведено в указанный период (`${date1}` – `${date2}`),
4. Пациент прошёл **колоноскопию** после выявления высокого ПСА,
5. После колоноскопии был установлен **диагноз из заданного списка МКБ-10**, включающего:
   - Злокачественные новообразования толстой кишки и прямой кишки (`C18–C21.8`),
   - Доброкачественные новообразования толстой кишки (`D12–D12.9`),
   - Воспалительные заболевания кишечника (`K50–K52.9`),
   - Полипы, язвы и перфорации кишечника (`K62.0`, `K62.1`, `K63.5`).

Результат — количество таких случаев **по каждому медицинскому учреждению (МО)** из заранее заданного списка.

> 💡 Скрипт написан для Oracle Database и использует коллекции (`SYS.ODCIVARCHAR2LIST`) и материализованные подзапросы (`/*+ MATERIALIZE */`) для повышения читаемости и производительности.

## 🧩 Используемые коды услуг и диагнозов

- **ПСА**: код услуги `A09.19.001`
- **Колоноскопия**: код услуги `A03.18.001`
- **Диагнозы**: диапазоны и коды по МКБ-10 (см. запрос)

## 📂 Структура запроса

1. **`target_lpu`** — список целевых МО по коду.
2. **`psa_high`** — отбор пациентов с ПСА ≥ 100 в нужном возрасте и периоде.
3. **`colono_after_psa`** — фильтрация тех, кто прошёл колоноскопию *после* анализа ПСА.
4. **`diag_after_colono`** — отбор пациентов с релевантными диагнозами *после* колоноскопии.
5. **Финальный SELECT** — агрегация по МО с использованием `LEFT JOIN`, чтобы включить даже те МО, где случаев не найдено.

## ⚙️ Параметры

- `${date1}` — начальная дата периода (формат `dd.mm.yyyy`)
- `${date2}` — конечная дата периода (включительно)

> Эти параметры можно заменить при запуске через DBeaver, SQL*Plus или другой клиент.

## 📌 Примечания
- Использование `INTERVAL '1' DAY - INTERVAL '1' SECOND` позволяет включить весь день `${date2}`.

---

Автор: [Дмитрий Чекуленко]
